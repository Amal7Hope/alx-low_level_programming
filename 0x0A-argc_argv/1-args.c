#include <stdio.h>
/**
 * main - writr a program that pronts the number of argument
 * @argc: this is the argument count
 * @argv: this is the argument vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	(void)argv;
	return (0);

}
